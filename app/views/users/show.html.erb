<h1><%= @user.email %>'s Events</h1>

<ul>
  <% @created_events.each do |event| %>
    <li>
      <%= link_to event.name, event_path(event) %> - <%= event.date.strftime("%B %d, %Y") %> - <%= event.location %>
    </li>
  <% end %>
</ul>